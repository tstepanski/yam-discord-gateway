/**
 * Sticker Types
 *
 * @see [Sticker Types](a sticker uploaded to a guild for the guild's members)
 *
 * @enum
 */
export enum Type {
	/**
	 * an official sticker in a pack
	 */
	STANDARD = 1,

	/**
	 * a sticker uploaded to a guild for the guild's members
	 */
	GUILD = 2
}